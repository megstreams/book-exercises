# Create a variable `hometown` that stores the city in which you were born
hometown<- "Quakertown"
# Assign your name to the variable `my_name`
my_name<-"Meg Streams"
# Assign your height (in inches) to a variable `my_height`
my_height<-5.5
# Create a variable `puppy_price`, which is how much you think a puppy costs
puppy_price<-100
# Create a variable `total_cost` that has the total cost of all of your puppies
total_cost<-puppy_price * puppies
# Create a variable `total_cost` that has the total cost of all of your puppies
total_cost<-puppy_price * puppies
# Create a variable `total_cost` that has the total cost of all of your puppies
total_cost<-puppy_price*puppies
# Create a variable `puppies` equal to the number of puppies you'd like to have
puppies<-2
# Create a variable `total_cost` that has the total cost of all of your puppies
total_cost<-puppy_price*puppies
# Create a variable `hometown` that stores the city in which you were born
hometown<- "Quakertown"
# Create a boolean variable `too_expensive`, set to TRUE if the cost is greater
# than $1,000
too_expensive<-TRUE if(total_cost>1000)
# Create a boolean variable `too_expensive`, set to TRUE if the cost is greater
# than $1,000
too_expensive<-TRUE  (total_cost>1000)
# Create a boolean variable `too_expensive`, set to TRUE if the cost is greater
# than $1,000
if(total_cost>1000) too_expensive<-TRUE else too_expensive<-FALSE
# Create a variable `max_puppies`, which is the number of puppies you can
# afford for $1,000
max_puppies<-total_cost/puppy_price
# Create a variable `max_puppies`, which is the number of puppies you can
# afford for $1,000
max_puppies<-1000/puppy_price
# Create a variable `max_puppies`, which is the number of puppies you can
# afford for $1,000
max_puppies<-floor(1000/puppy_price)
# Create a variable `max_puppies`, which is the number of puppies you can
# afford for $1,000
max_puppies<-floor(1000/puppy_price)
# Create a variable `puppy_price`, which is how much you think a puppy costs
puppy_price<-150
# Create a variable `max_puppies`, which is the number of puppies you can
# afford for $1,000
max_puppies<-floor(1000/puppy_price)
print(1000/150)
# Exercise 1: practice with basic R syntax
# Create a variable `hometown` that stores the city in which you were born
hometown<- "Quakertown"
# Assign your name to the variable `my_name`
my_name<-"Meg Streams"
# Assign your height (in inches) to a variable `my_height`
my_height<-5.5
# Create a variable `puppies` equal to the number of puppies you'd like to have
puppies<-2
# Create a variable `puppy_price`, which is how much you think a puppy costs
puppy_price<-150
# Create a variable `total_cost` that has the total cost of all of your puppies
total_cost<-puppy_price*puppies
# Create a boolean variable `too_expensive`, set to TRUE if the cost is greater
# than $1,000
if(total_cost>1000) too_expensive<-TRUE else too_expensive<-FALSE
# Create a variable `max_puppies`, which is the number of puppies you can
# afford for $1,000
## used floor function to ensure not dealing in fractions of puppies or busting
#budget
max_puppies<-floor(1000/puppy_price)
print(1000/150)
